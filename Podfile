# Uncomment this line to define a global platform for your project
# platform :ios, '6.0'
use_frameworks!

target 'Pods-Target' do
	pod 'DCIntrospect-ARC', '~> 0.0', :configurations => ['ConfigTest']
end

